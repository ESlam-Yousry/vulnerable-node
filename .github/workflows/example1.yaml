name: example1
on:
  push:
    branches: master
jobs:     
  build:
    name: Build
    runs-on: self-hosted
    steps:
    - name: checkout
      uses: actions/checkout@v2
    - name: Hello  
      run: |
        echo "Hello world from self hosted runner!" 
