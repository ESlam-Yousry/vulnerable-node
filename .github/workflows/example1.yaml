name: example1
on:
  push:
    branches: [main]
jobs:     
  build:
    name: Build
    runs on:
    steps:
    - name: checkout
      uses: actions/checkout@v2
    - name: Hello  
      run: |
        echo "Hello world from self hosted runner!" 
